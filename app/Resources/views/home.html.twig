{% extends 'layout.html.twig' %}

{% block maincontent %}
    <div class="tdc-content-wrap">
        {#include block evidenced #}
        {{ render(controller('AppBundle:Blocks:evidenced')) }}
        {#===================================#}

        <div class="tdc-row">
            <div class="td-pb-row" >
                <div class="td-pb-span8 td-main-content" role="main">
                    {#include block main articles #}
                    {{ render(controller('AppBundle:Blocks:mainArticles')) }}
                    {#===================================#}
                </div>
                <div class="td-pb-span4 td-main-sidebar" role="complementary">
                    {#include sidebar #}
                    {% include 'sidebar.html.twig' %}
                    {#===================================#}
                </div>
            </div>
        </div>
        {#include block related #}
        {#{ render(controller('AppBundle:Blocks:related')) }#}
        {#===================================#}

    </div>
{% endblock %}

